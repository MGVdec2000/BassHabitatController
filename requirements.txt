# Core computer-vision and numeric packages
opencv-python-headless>=4.5.0
numpy>=1.21.0
matplotlib>=3.4.0

# Optional: Gaussian smoothing for the occupancy heatmap.
# Remove if scipy is too heavy for your Pi installation; the heatmap
# will still work, just without smoothing.
scipy>=1.7.0

# GPIO stack (Raspberry Pi)
# RPi.GPIO is used first; on Pi 5, rpi-lgpio/lgpio is used as a fallback backend.
RPi.GPIO>=0.7.1
rpi-lgpio>=0.6

opencv-python